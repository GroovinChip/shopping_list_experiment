name: shopping_list
description: Shopping list experiment
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cloud_firestore: ^0.16.0+1
  cupertino_icons: ^1.0.2
  firebase_core: ^0.7.0
  provider: ^4.3.3
  rxdart: ^0.25.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:

  uses-material-design: true
